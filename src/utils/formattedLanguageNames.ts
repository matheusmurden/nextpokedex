import { type SupportedLanguagesMap } from "types/index";

export const formattedLanguageNames: Record<keyof SupportedLanguagesMap, string> = {
    ja: 'japanese',
    en: 'english',
    es: 'spanish',
    fr: 'french',
    de: 'deustch'
}